export interface Frequencies {
  daily: number;
  monthly: number;
  quarterly: number;
  yearly: number;
}

export const FREQUENCIES: Frequencies = {
  daily: 365,
  monthly: 12,
  quarterly: 4,
  yearly: 1
}