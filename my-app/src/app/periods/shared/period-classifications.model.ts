export interface PeriodClassifications {
  month_s: string;
  quarter_s: string;
  year_s: string;
}

export const PERIODCLASSIFICATIONS: PeriodClassifications = {
  month_s: "month(s)",
  quarter_s: "quarter(s)",
  year_s: "year(s)"
}